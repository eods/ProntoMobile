@model ProntoMobile.Web.Models.EditUserViewModel
@{
    ViewData["Title"] = "Edit";
}

<h2>Editar</h2>

<h4>Administrador</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />

            <partial name="_User" />

            <div class="form-group">
                <input type="submit" value="Aceptar" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-success">Volver a Administradores</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
